interface ContactEmailProps {
    name: string;
    email: string;
    phone?: string;
    subject: string;
    message: string;
}
export declare function contactEmailTemplate({ name, email, phone, subject, message, }: ContactEmailProps): string;
export {};
//# sourceMappingURL=contact-email.d.ts.map